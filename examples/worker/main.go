package main

func main() {
	// manager := worker.NewManager(context.Background())
	// now := time.Now()
	// workers, err := manager.GetOnlineWorkersFromSB()
	// if err != nil {
	// 	panic(err)
	// }
	// for _, w := range workers {
	// 	log.Printf("Worker ID: %s, Address: %s, GRPC Port: %d", w.WorkerID, w.Address, w.GRPCPort)
	// }
	// diff := time.Since(now)
	// log.Printf("Fetched %d online workers in %s", len(workers), diff)
}
